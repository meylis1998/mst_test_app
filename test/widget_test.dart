import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Add proper widget tests once the app is set up.
    expect(true, isTrue);
  });
}
